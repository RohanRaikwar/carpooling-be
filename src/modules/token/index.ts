import {
  revokeRefreshToken,
  generateTokens,
  verifyAccessToken,
  verifyRefreshToken,
} from './tokens.service';
export default { revokeRefreshToken, generateTokens, verifyAccessToken, verifyRefreshToken };
